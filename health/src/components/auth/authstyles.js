import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles((theme) => ({
    title: {
      flexGrow: 1,
    },
    btn:{
        marginRight : "1.5em",
    },

    link:{
        textDecoration:"none",
        color:"black"
    }
  }));
  export  {useStyles}